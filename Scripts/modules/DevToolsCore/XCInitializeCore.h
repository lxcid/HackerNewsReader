#import <Foundation/Foundation.h>

extern void IDEInitialize(BOOL arg1, void *arg2);
// The parameter is whether UI must be initialized (which we don't need)
extern void XCInitializeCoreIfNeeded(BOOL arg1);